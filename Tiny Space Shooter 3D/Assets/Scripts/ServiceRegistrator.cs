using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity;

public class ServiceRegistrator : MonoBehaviour
{
    private void Start()
    {
        //ServiceLocator.RegisterContainer<Level>("Level", FindObjectOfType<Level>());
        //ServiceLocator.RegisterContainer<EnemySpawner>("EnemySpawner", FindObjectOfType<EnemySpawner>());
        //ServiceLocator.RegisterContainer<UiHandler>("UiHandler", FindObjectOfType<UiHandler>());
        //ServiceLocator.RegisterContainer<PlayerController>("PlayerController", FindObjectOfType<PlayerController>());

    }
}